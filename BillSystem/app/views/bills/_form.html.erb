<%= form_with(model: bill, local: true) do |form| %>

  <div class="form-group">
    <%= form.label :name%>
    <%= form.text_field :name, id: :bill_note, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>