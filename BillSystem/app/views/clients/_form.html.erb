<%= form_with(model: client, local: true) do |form| %>
  <% if client.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(client.errors.count, "error") %> prohibited this client from being saved:</h2>

      <ul>
      <% client.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :client_name %>
  </div>

  <div class="field">
    <%= form.label :surename %>
    <%= form.text_field :surename, id: :client_surename %>
  </div>

  <div class="field">
    <%= form.label :fiscalCode %>
    <%= form.text_field :fiscalCode, id: :client_fiscalCode %>
  </div>

  <div class="field">
    <%= form.label :hourTariff %>
    <%= form.number_field :hourTariff, id: :client_hourTariff %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
