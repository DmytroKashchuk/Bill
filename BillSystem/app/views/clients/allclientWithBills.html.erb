    <h1>Clients</h1>
    <table class="table table-striped table-inverse">
      <thead class="thead-dark">
        <tr>
          <th>#</th>
          <th>Name</th>
          <th>Surename</th>
          <th>Fiscalcode</th>
          <th>Hourtariff</th>
        </tr>
      </thead>

      <tbody>
        <% @clients.each do |client| %>
          <tr>
            <th scope="row"><%@client_id%></th>
            <th><%= client.name %></th>
            <th><%= client.surename %></th>
            <th><%= client.fiscalCode %></th>
            <th><%= client.hourTariff %></th>
          </tr>
        <% end %>
      </tbody>
    </table>

    <br>
    <div class="btn-group btn-group-justified" role="group" width="100%">
      <%= link_to 'New client', new_client_path, class: "btn btn-primary btn-lg active"%>
    </div>

<div class="btn-group btn-group-justified" role="group">
  
</div>